1. 更新 API Service
在 lib/services/ai_service.dart（如果不存在请新建）中，添加一个异步方法 generateAnimationScript(String prompt)：

发送 HTTP POST 请求到后端接口。

注意：基础 URL 请从配置文件或统一个地方读取。如果在 Android 模拟器测试，请使用 http://10.0.2.2:5001/api/v1/chat/animation（假设后端运行在宿主机的 5001 端口）。

解析响应时，务必提取 jsonDecode(response.body)['data']['script']，然后使用 AnimationScript.fromJson 将其转换为 Dart 对象并返回。

2. 在课件预览页添加入口
修改 lib/pages/courseware_viewer_page.dart：

在页面的右下角添加一个 FloatingActionButton (图标可以是 Icons.auto_awesome，代表 AI)。

点击该按钮时，弹出一个 showModalBottomSheet 或者 AlertDialog。

弹窗内包含一个 TextField（供用户输入想看的动画提示词，例如“演示快速排序”）和一个“生成动画”按钮。

3. 连接状态与画布展示
处理“生成动画”按钮的点击逻辑：

点击后，显示 CircularProgressIndicator（Loading 状态），提示“AI 正在思考并编写动画剧本...”。

调用 ai_service.generateAnimationScript 获取数据。

获取成功后，关闭输入弹窗，并弹出一个新的全屏 Dialog 或覆盖层，在这个层里实例化我们之前写好的 AnimationCanvasWidget，并将获取到的真实的 AnimationScript 对象传给它播放！

做好异常捕获，如果网络请求失败或 AI 生成超时，通过 SnackBar 提示用户。

输出要求
请提供修改后的 Dart 代码片段。要求代码健壮，具有良好的加载状态管理和错误提示。