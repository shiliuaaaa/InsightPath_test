# 核心开发指令（请按顺序执行）

### 第一步：引入依赖 (`pubspec.yaml`)
检查并添加 Markdown 渲染库，因为 AI 的回复包含加粗、代码块等 Markdown 格式：
- 添加依赖：`flutter_markdown: ^0.7.1` (或最新版本)。
- 运行 `flutter pub get`。

### 第二步：更新服务层 (`ai_service.dart`)
在 `AiService` 中新增对接 Java 后端“全局聊天”的方法（请根据后端的实际返回结构解析数据）：
1. `createGlobalSession()`: POST 请求创建新会话，返回 `sessionId`。
2. `getGlobalSessions()`: GET 请求获取用户的历史会话列表。
3. `getGlobalMessages(String sessionId)`: GET 请求获取某个会话的历史消息列表。
4. `sendGlobalMessage(String sessionId, String content)`: POST 请求发送消息并获取 AI 的最新回复。

### 第三步：创建极简聊天页 (`global_ai_tutor_page.dart`)
新建该页面，必须严格遵循以下视觉和交互规范：
1. **整体布局**：
   - 页面背景色使用极浅灰 (`#F8F9FA`)。
   - **AppBar**：使用透明背景 + `BackdropFilter` (高斯模糊) 实现毛玻璃效果。标题为“小犀导师”。右上角放置一个 `IconButton(Icons.history)`，点击后从右侧滑出一个 `Drawer` 展示历史会话列表。
2. **消息列表 (ListView)**：
   - 使用 `ListView.builder` 展示对话。添加适当的 padding 让气泡“呼吸”。
   - **用户气泡**：靠右。背景色使用应用主色调（智慧蓝 `#1A73E8`），文字纯白。大圆角（例如 `BorderRadius.circular(20)`），但右下角为小圆角 (`Radius.circular(4)`)。
   - **AI气泡**：靠左。背景色纯白，带极其微弱的阴影（Elevation 0.5）。使用 `MarkdownBody` 组件渲染内容。大圆角，左下角为小圆角。
3. **底部输入区**：
   - 固定在底部，带有向上的极淡渐变阴影或毛玻璃背景。
   - 输入框：使用无边框、带有灰色底色（`#EEEEEE`）的 `TextField`，大圆角 (`circular(24)`)，内部提示词“向小犀提问...”。
   - 发送按钮：放在输入框右侧。当输入框为空时为灰色不可点击；有文字时变为蓝色。
4. **状态管理**：
   - 页面 `initState` 时，如果没有 `sessionId`，先调用 `createGlobalSession`。
   - 用户点击发送后：立即把用户消息加入列表并 `setState`，清空输入框，列表自动滚动到底部。
   - 此时在列表末尾显示一个 Loading 状态（如 CupertinoActivityIndicator 配合“小犀正在思考...”文字）。
   - 收到 AI 回复后：移除 Loading 状态，将 AI 消息加入列表，再次滚动到底部。

### 第四步：集成到底部导航栏 (`student_home_page.dart`)
重构包含 `BottomNavigationBar` 的主页面：
- 将底部菜单项设计为 3 个：
  1. 【学习】(图标 `Icons.menu_book`) -> 对应之前的课程列表页
  2. 【AI 导师】(图标 `Icons.smart_toy_outlined`) -> 对应刚才新建的 `GlobalAiTutorPage`
  3. 【我的】(图标 `Icons.person_outline`) -> 对应个人中心
- 确保切换 Tab 时状态不丢失（可以使用 `IndexedStack` 或 `PageView`）。

# 输出要求
请一步步给出修改后的 Dart 代码。特别注意 `GlobalAiTutorPage` 中的 UI 细节设定（毛玻璃、气泡圆角、Markdown 渲染），务必达到商业 App 样片标准。