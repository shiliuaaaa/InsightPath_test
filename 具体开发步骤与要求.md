# 角色与任务
你是一位顶级的全栈架构师。我们现在要实现《项目计划书》中最核心的“预设知径（干预策略）”功能。
在这个功能中：教师可以在课件（PDF）的特定页码预设 AI 动画剧本并存入数据库；学生浏览时，优先播放老师预设的动画，如果没有预设，则允许学生现场调用大模型生成（不保存）。

# 具体开发指令：

### 第一步：后端接口补全
1. 在后端的 Controller（负责 `SectionAiConfig`）中，新增/检查两个接口：
   - `POST /api/v1/sections/{sectionId}/ai-configs`：教师保存预设。接收 `pageNumber`, `prompt`, `generatedDsl` (JSON字符串)，保存到 `section_ai_configs` 表。
   - `GET /api/v1/sections/{sectionId}/ai-configs`：获取该课件下所有的预设配置列表。

### 第二步：前端 AI Service 更新
在 `ai_service.dart` 中增加对应的方法：
- `savePresetConfig(String sectionId, int pageNumber, String prompt, String dsl)`
- `Future<List<SectionAiConfig>> getPresetConfigs(String sectionId)`

### 第三步：重构课件浏览页 (`courseware_viewer_page.dart`)
引入“教师/学生双轨模式”。
1. **状态初始化**：页面加载时，获取当前登录用户的角色 (`Role.TEACHER` 还是 `STUDENT`)，并调用 `getPresetConfigs` 获取该课件所有预设锚点。监听 PDF 翻页事件，记录当前的 `currentPageNumber`。

2. **教师端视角 (Teacher Mode)**：
   - 在右下角的 FAB 按钮变为 **“+ 预设知径”**。
   - 点击后弹出一个毛玻璃 BottomSheet，教师输入提示词（如“演示冒泡排序的交换”）。
   - 点击“生成预览” -> 调用 AI 接口生成 DSL，并在面板内实例化 `AnimationCanvasWidget` 预览。
   - 预览下方出现一个**“保存预设到当前页”**按钮。点击后，调用 `savePresetConfig` 将这段 JSON 永久绑定在当前页码。

3. **学生端视角 (Student Mode) - 智能路由**：
   - 当学生翻到某一页时，检查获取到的 `presetConfigs` 列表中，是否有对应 `currentPageNumber` 的预设数据。
   - **如果有预设**：右下角出现一个发光的、带呼吸动效的特殊按钮（例如标有“老师留下的锦囊”或“播放演示”）。点击后，**直接秒开**毛玻璃卡片，使用老师预设好的 JSON 剧本渲染 `AnimationCanvasWidget`，无需请求大模型。
   - **如果没有预设**：右下角显示普通的“AI 助教”按钮。点击后，按照我们之前的逻辑，由学生手动输入疑问，临时调用大模型生成动画进行解答（这部分不存数据库）。

# 输出要求
请提供前后端相关的代码更新，特别是 `courseware_viewer_page.dart` 中针对“教师与学生”、“预设与现场生成”逻辑路由的精彩实现。要求 UI 必须延续之前的沉浸式毛玻璃极简风格。